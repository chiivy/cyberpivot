# CyberPivot CLI

Python companion for lab setup and system checks. Windows-friendly.

```bash
cd cli
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python -m checks.system_check
```
